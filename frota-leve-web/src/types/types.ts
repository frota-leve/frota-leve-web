export interface Employee {
  id: string;
  name: string;
  email: string;
  document: string;
  role: string;
}

export interface Car {
  id: string;
  name: string;
  model: string;
  plate: string;
  brand: string;
}